# Issue templates here are for internal use. Clients never visit this repo's
# Issues tab — they submit via the intake form, which calls the GitHub API
# directly. Blank issues are disabled so accidental clicks don't create noise.
blank_issues_enabled: false
